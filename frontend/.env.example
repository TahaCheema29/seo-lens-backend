# Browser: Stripe publishable key
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_...

# Server-side only: FastAPI base URL (no trailing slash). Used by /api/* route handlers.
BACKEND_URL=http://127.0.0.1:8000
